[project]
name = "sieveofatkin"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bitarray>=3.0.0",
]
[tool.ruff.lint]
select = [
    "D",
    "PTH",
]
