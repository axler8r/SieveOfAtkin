#! /usr/bin/env zsh

local _from _to
if [[ $# -eq 2 && "$1" =~ ^[0-9]+$ && "$2" =~ ^[0-9]+$ ]]; then
    _from="$1"
    _to="$2"
else
    unset _from _to
fi

if [[ -n $_from && -n $_to ]]; then
    python3 main.py list --start $_from --stop $_to
else
    echo "Usage: lsprime <from> <to>"
fi
