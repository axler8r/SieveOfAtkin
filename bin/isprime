#! /usr/bin/env zsh

local _number
if [[ $# -eq 1 && "$1" =~ ^[1-9][0-9]*$ ]]; then
    _number="$1"
else
    unset _number
fi

if [[ -n $_number ]]; then
    PYTHONPATH="$(dirname $"0")/sieve" python3 -m sieve.main check --number $_number
else
    echo "Usage: isprime <number>"
fi
